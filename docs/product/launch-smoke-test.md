# 上线前冒烟测试和回滚方案

适用范围：根目录静态 MVP 主体验（源码入口为 `index.html`、`src/`、`public/`，发布产物为 `dist-static/`）。`apps/web/` 不属于本次种子版本入口。
目标：上线前用最短路径确认目标用户能从空白状态建立初步组合、理解整体资产视图和数据状态，并且导入恢复、合规和隐私边界没有阻断风险。

## 通过标准

- 首页能通过 HTTPS 打开，无白屏、无控制台阻断错误。
- 用户可以从空白状态添加至少两个资产，并在总览理解组合总值、主要持仓和至少两个实际分布维度。
- 用户可以识别正常、缺失、过期、手动、估算、缓存和错误状态。
- 价格缺失、估算图表、导入覆盖、本地存储风险都有明确提示。
- 页面不出现收益承诺、保本暗示、跟单引导、个性化投资建议或真实隐私样例。
- JSON 导出、错误文件拦截、导入预览、取消、确认和损坏数据恢复路径实测通过。
- `npm test`、`npm run typecheck`、`npm run build:static`、`git diff --check` 全部通过。

## 自动检查

上线前在仓库根目录执行：

```bash
npm test
npm run typecheck
npm run build:static
git diff --check
```

如果本地静态服务已启动，检查核心资源：

```bash
curl -I http://127.0.0.1:4175/
curl -I http://127.0.0.1:4175/src/app.js
curl -I http://127.0.0.1:4175/src/styles/app.css
```

检查敏感和合规文案：

```bash
rg -n "收益承诺|保本|跟单|推荐买|推荐卖|真实账户|身份证|secret|api_key|password|token" . --glob '!node_modules/**' --glob '!apps/web/dist/**' --glob '!package-lock.json'
```

允许出现的结果：

- 文档中的合规禁用词清单。
- 风险提示里的“不构成投资建议”。
- 环境变量示例或行情脚本中的占位密钥字段名。

需要拦截上线的结果：

- 产品页面承诺收益、保本、跟单或推荐具体买卖。
- 真实账户、真实交易记录、真实身份证明或真实密钥。
- 未脱敏的邮箱、手机号、交易所 API Key 或券商凭证。

## 桌面端核心路径冒烟

建议宽度：1280px。

- 从空白状态打开首页，确认首个资产添加入口清楚且可点击。
- 确认页面能看到本地存储、备份、风险提示和数据来源说明。
- 新增一笔股票或 ETF，不填写当前价，确认状态显示待补价格，估值按成本价或明确标注。
- 新增一笔现金资产，确认表单切换到现金金额语义，账户和币种正常保存。
- 回到总览，确认组合总值、主要持仓和至少两个实际分布维度可以理解和复算。
- 确认正常、缺失、过期、手动、估算、缓存和错误状态不会相互混淆。
- 导出 JSON 备份，确认文件可以下载。
- 粘贴错误 JSON 导入，确认失败信息明确。
- 粘贴有效 JSON 导入，确认先展示覆盖预览和差异，再由用户确认覆盖。
- 取消一次有效导入，确认现有数据没有被替换；再次确认导入，确认数据正确恢复。
- 使用损坏本地数据场景，确认错误告知、原始内容下载和有效备份恢复路径可完成。
- 点击页脚“用户反馈”，确认反馈弹窗可打开、可复制模板，邮件草稿收件人为 `jennieshen1027@gmail.com`。

## 桌面端次级功能回归

以下功能保留可见，但不属于首次种子任务成功门槛：

- 对一笔持仓记录加仓、减仓，再清仓，确认数量、成本、已实现收益和历史持仓正常。
- 清仓后点击“去写复盘笔记”，确认进入清仓复盘模板并关联资产。
- 打开分析页，确认趋势、归因、贡献、回撤等结果都有数据口径标签。
- 打开复盘页，确认内容仍明确为可选的私人记录。

## 移动端手动冒烟

建议宽度：390px 和 768px。

- 从空白状态打开总览，确认顶部、导航、指标和首个资产主操作无重叠。
- 打开资产表单，确认字段单列展示，输入框、选择器、提交按钮可点击。
- 添加至少两个资产，确认可以返回总览理解组合总值、主要持仓和分布。
- 查看持仓表，确认表格可横向滚动，关键列不会被压到不可读。
- 查看分析页，确认图表标题、数据口径、空状态和说明文字不互相遮挡。
- 打开复盘编辑器，确认标题、正文、标签、资产关联和保存按钮可用。
- 打开反馈弹窗，确认在手机宽度下内容可滚动，复制和邮件按钮可点击。

## 上线后 5 分钟检查

- 打开线上首页，确认首屏正常渲染。
- 新增一笔虚构资产，刷新页面后确认本地数据仍保留。
- 导出 JSON，再用错误 JSON 验证导入失败提示。
- 打开反馈入口，确认邮件地址和反馈模板正确。
- 再次确认页面没有真实密钥、真实账户、真实交易记录或收益承诺。

## 回滚方案

静态 MVP 优先使用托管平台的上一个成功部署版本回滚。

1. 保留每次上线前的构建产物、部署版本号或 commit 标识。
2. 上线后若出现白屏、数据丢失、无法保存、无法导出、严重计算错误或严重合规文案，立即停止扩散链接。
3. 在部署平台回滚到上一个通过冒烟测试的版本。
4. 回滚后重新执行“上线后 5 分钟检查”。
5. 将问题记录到 roadmap 或 issue，修复后补充可复现测试，再重新发布。

不建议用临时热修绕过导入导出、持仓计算或风险提示；这些属于信任基础，修复必须可复算、可测试。

## 当前执行记录

日期：2026-06-29  
执行环境：本地仓库，Asia/Shanghai。

- 自动测试：通过，`npm test` 共 18 项通过。
- 类型检查：通过，`npm run typecheck` 覆盖 `packages/domain` 和 `apps/web`。
- 构建：通过，执行记录当时使用 `npm run build` 完成 `apps/web` Vite 构建；该历史结果不代表当前根目录静态种子入口已通过 `npm run build:static`。
- 静态资源检查：通过，`/`、`/src/app.js`、`/src/styles/app.css` 在 `http://127.0.0.1:4175/` 返回 200。
- 合规和敏感词扫描：通过，无上线阻断；命中项为合规禁用词清单、风险提示、反馈隐私提醒、环境变量占位字段和行情脚本备用 `api_key` 参数。
- 浏览器截图级桌面和移动端检查：待人工或浏览器工具执行。

日期：2026-06-30  
执行环境：本地仓库，Asia/Shanghai。

- 本地静态服务：通过，`http://127.0.0.1:4175/` 返回 200。
- 关键 DOM 结构检查：通过，首页、资产、分析、复盘、导入确认弹窗、清仓复盘弹窗和反馈弹窗入口存在。
- 反馈入口检查：通过，页脚按钮存在，邮件收件人为 `jennieshen1027@gmail.com`，反馈表单包含联系方式、反馈内容、复制和邮件发送入口。
- 导入预览结构检查：通过，导入确认弹窗包含 `#import-preview`，导入前可展示覆盖预览。
- 响应式结构检查：通过，CSS 覆盖 900px、768px、620px、430px 等断点，资产表单、导入预览、导航、表格和弹窗有单列或横向滚动规则。
- 截图生成：未完成。当前环境没有 Playwright 依赖；两次尝试使用本机 Chrome headless 生成 1280px 桌面和 390px 移动端截图，均因系统权限审核超时未执行。
- 结论：结构级冒烟通过；截图级视觉验收仍需在可授权 Chrome headless、Playwright 或人工浏览器环境中补跑，不能标记为最终通过。

日期：2026-07-16
执行环境：本地仓库，候选版本 `985a3e9`，Asia/Shanghai。

- 自动测试：通过，`npm test` 共 89 项通过。
- 类型检查：通过，`npm run typecheck` 覆盖 `packages/domain` 和 `apps/web`。
- 当前静态入口构建：通过，`npm run build:static` 生成 `dist-static/`。
- Diff 检查：通过，`git diff --check` 无异常。
- 浏览器核心路径、三档响应式、导入恢复和损坏恢复交互：尚未执行；当前环境没有可用浏览器实例，不能以结构检查或自动测试替代。
- 结论：自动门禁通过；当前版本仍未满足最终发布条件。

日期：2026-07-25
执行环境：独立工作树 `release/seed-2026-07-25`，候选代码确认点 `2996e75`，上一候选基线 `473d368`，Asia/Shanghai。

- 候选隔离：通过。产品负责人已确认将 `fc192ef` 的运行时公共资产发现、自动买入价和自动同步改动纳入本候选；对应 ADR 0011 已批准为 `Accepted`。
- 首页收益边界：已修正手动价格、缺失上一期汇率和纯现金组合不应显示为可核对交易日收益的问题；不可计算原因改为页面直接可见。
- 自动发布门禁：通过，`npm run verify:release` 共 119 项测试通过，类型检查、React 并行入口安全构建和根目录静态构建均通过。
- Diff 检查：通过，`git diff --check` 无异常。
- 生产迁移准备：已增加 `market_data_sync_targets` 的范围化迁移和只读验证 SQL；尚未连接或修改生产数据库。
- 浏览器核心路径、390px、768px、1280px、导入恢复和损坏恢复交互：尚未执行。浏览器运行环境返回无可用会话，不能以自动测试替代。
- 部署状态：尚未确认 Supabase、Render、Vercel 生产项目、环境变量、部署地址或上一可回滚版本。
- 结论：候选代码确认点 `2996e75` 和自动门禁通过；最终冻结点以本记录提交后的分支 HEAD 为准。浏览器、生产环境、部署和回滚门禁未完成，不得发布。

日期：2026-07-25
执行环境：独立工作树 `release/seed-2026-07-25`，浏览器验收基线 `7f8df00`，修复后候选代码确认点 `2c4d77a`，Asia/Shanghai。

- 三档首次组合路径：1280px 的空状态、两笔虚构资产录入、总览复算、主要持仓和类别/账户分布通过；768px 的总览、持仓表横向滚动、分析页和复盘编辑器通过；390px 的总览、导航、单列表单、分析页和反馈弹窗可用。
- 浏览器验收阻断：基线 `7f8df00` 的现金资产表单仍显示“股数”；390px 复盘编辑器被透明标签 checkbox 撑宽至 607px。两项问题已在 `2c4d77a` 做最小修复并补充回归测试，尚待浏览器复验。
- 导入与反馈：错误 JSON 拦截、有效 JSON 覆盖预览、取消不替换、确认恢复均通过；反馈弹窗三档可打开，复制成功，邮件地址正确。控制台无错误或警告。
- 尚未完成：JSON 导出按钮无控制台错误，但浏览器未捕获下载事件，尚不能确认文件落盘；损坏 `localStorage` 的原始内容下载和备份恢复交互未实测。
- API 与边界：健康检查返回 `{"ok":true}`，公共资产搜索 `510300` 返回沪深300ETF；未使用 React 入口，未部署生产，未连接或修改生产数据库，测试资产均为虚构数据。
- 修复后自动门禁：`npm run verify:release` 共 120 项测试通过，类型检查、React 并行入口安全构建、根目录静态构建和 `git diff --check` 均通过。
- 结论：`2c4d77a` 不能直接继承 `7f8df00` 的浏览器通过项。需复验现金字段、390px 复盘宽度、JSON 下载落盘和损坏数据恢复后，再决定是否放行。

日期：2026-07-26
执行环境：独立工作树 `release/seed-2026-07-25`，浏览器复验基线 `9739226`，Asia/Shanghai。

- 受影响 UI 复验：现金资产选择后字段切换为“现金金额”，占位示例为 `50000`；390px 复盘页的视口、文档和页面宽度均为 390px，未发现越界元素。两项通过。
- 下载复验：JSON 导出和“下载损坏数据”按钮均可点击，但内置 Browser 未捕获下载事件，Downloads 和常用临时目录均无落盘文件。该结果不能证明普通 Chrome 必然失败，也不能作为下载通过证据。
- 损坏数据恢复：错误告知、有效备份预览、确认恢复和重新加载通过；恢复后首页显示虚构资产和 CNY 220.00，但控制台出现 `Cannot convert null to a BigInt`，因此恢复流程仅部分通过。
- 根因与修复：无成本基础的恢复资产会产生合法的 `returnBps = null`，分析模型此前仍将其传入 `BigInt` 计算年化收益。现已保留“收益率暂无数据”语义并增加完整分析模型回归测试。两处下载共用逻辑改为点击后延迟释放 Blob URL，降低下载尚未开始即失效的兼容性风险；导出格式未改变。
- 修复后自动门禁：124 项测试全部通过；类型检查、React 并行入口安全构建、根目录静态构建和 `git diff --check` 通过。首次全量测试在受限沙箱内因临时 API 端口监听 `EPERM` 失败，使用允许本地监听的测试权限重跑后 API smoke 及全部测试通过。
- 结论：分析恢复异常已有自动化修复证据，但 JSON 备份和损坏数据文件仍需在普通 Chrome 中确认实际落盘、读取内容，并再次确认恢复后控制台干净。K03、I03、I04 继续保持未完成，不得进入最终发布审批。
- 最终复验结论（2026-07-26，候选版本 `d86ca7a3da253bd0f137cbfa81fda1663488fe3b`）：浏览器核心路径、三档响应式、JSON 导出和损坏数据恢复门禁通过，K03、I03、I04 完成。生产配置、部署、线上冒烟和回滚门禁仍未完成，本结论不构成最终生产发布批准。
